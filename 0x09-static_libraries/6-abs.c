#include<stdio.h>
#include<stdlib.h>
#include "main.h"
/**
 * _abs - to be executed
 *
 * @n: to be checked
 *
 * Return: value is 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
