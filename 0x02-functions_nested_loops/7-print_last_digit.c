#include<stdio.h>
#include<stdlib.h>
#include "main.h"
/**
 * print_last_digit - to be executed
 *
 * Return: value is 0
 */
int print_last_digit(int r)
{
	int last;

	last = r % 10;
	return (last);
}
